# Copy this file outside git or to a *.private.yaml path, then seal it with kubeseal.
apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-ddns-secret
  namespace: default
type: Opaque
stringData:
  CF_API_TOKEN: REPLACE_WITH_CLOUDFLARE_API_TOKEN
